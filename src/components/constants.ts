export enum VARIANT {
  PRIMARY = "PRIMARY",
  SECONDARY = "SECONDARY",
}

export enum MODE {
  DRAFT = "DRAFT",
  ADD_SECTION = "ADD_SECTION",
}

export enum SCREEN {
  EDITOR = "EDITOR",
  PREVIEW = "PREVIEW",
}
